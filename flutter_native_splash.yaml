# flutter_native_splash configuration for immediate branded splash screen
flutter_native_splash:
  # This removes the white screen during cold start
  color: "1A237E"  # Briffini brand color (deep blue)
  
  # Keep it simple for now - just the background color
  android: true
  ios: true
  web: false